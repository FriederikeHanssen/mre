manifest {
    name            = 'outsyntax-samplesheets'
    author          = 'Friederike Hanssen'
    description     = 'Writing a samplesheet + metadata with the workflow output syntax'
    nextflowVersion = '!>=25.04.0'
    version         = '1.0.0'
}

// Everything the output block publishes lands here.
outputDir = 'results'
